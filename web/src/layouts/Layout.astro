---
interface Props {
  title: string;
  description?: string;
}

const {
  title,
  description = "Temporarily limit the size of data fields at the consensus level to protect Bitcoin's purpose as money.",
} = Astro.props;
const siteName = "BIP-110";
const canonicalUrl = Astro.url.href;
const ogImageUrl = new URL("/og.png", Astro.url).href;
const ogImageWidth = "2372";
const ogImageHeight = "1312";
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <meta property="og:title" content={title} />
    <meta property="og:site_name" content={siteName} />
    <meta property="og:description" content={description} />
    <meta property="og:type" content="website" />
    <meta property="og:url" content={canonicalUrl} />
    <meta property="og:image" content={ogImageUrl} />
    <meta property="og:image:width" content={ogImageWidth} />
    <meta property="og:image:height" content={ogImageHeight} />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={ogImageUrl} />
    <meta name="twitter:image:width" content={ogImageWidth} />
    <meta name="twitter:image:height" content={ogImageHeight} />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <title>{title}</title>
    <style is:inline>
      html:not(.theme-ready) { visibility: hidden; }
    </style>
    <noscript>
      <style>
        html:not(.theme-ready) { visibility: visible !important; }
        [data-slot="accordion-content"] { display: block !important; height: auto !important; }
        [data-slot="accordion-trigger"] svg { display: none !important; }
      </style>
    </noscript>
    <script is:inline>
      (function() {
        try {
          var theme = localStorage.getItem('theme');
          var prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
          if (theme === 'dark' || (!theme && prefersDark)) {
            document.documentElement.classList.add('dark');
          }
        } catch (e) {}
        document.documentElement.classList.add('theme-ready');
      })();
    </script>
  </head>
  <body class="min-h-screen antialiased">
    <slot />
  </body>
</html>

<style is:global>
  @import '../styles/global.css';
</style>
